{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability padrão",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
